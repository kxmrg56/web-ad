# 精准广告投放生态系统 - 广告管理中台

本仓库是“精准广告投放生态系统”的核心管理站（.33 节点）。负责处理来自新闻、视频、购物三大站点的广告请求，并基于用户历史行为进行精准的画像投放。

## 🌟 已实现核心功能

### 1. 匿名用户跨域追踪 (Cross-Domain Tracking)
针对浏览器限制第三方 Cookie 的痛点，实现了 **URL 参数显式同步** 方案：
* [cite_start]**技术实现**：前端通过正则提取本地 `visitor_id`，并在 fetch 请求中通过 `&visitorId=...` 强制带入 [cite: 22]。
* **解决问题**：彻底解决了在不同 IP（.33, .38 等）之间 ID 无法识别导致的推荐“致盲”问题。

### 2. 用户画像与权重推荐算法 (Interest Profile)
后端采用 **Servlet + DAO** 架构，实现了基于权重的偏好匹配：
* [cite_start]**兴趣建模**：自动将前端频道（如：`politics`）映射为广告库标准分类（如：`教育`）并累积积分 [cite: 12, 14, 48]。
* **精准策略**：采用“画像优先”逻辑，如果用户历史兴趣分达到阈值，即使在不同频道也会优先推送其偏好广告（例如：政治新闻刷多了，去体育频道也会看到教育广告）。

### 3. 标准化广告分发 API
为视频网站和购物网站提供了标准化的 API 集成接口：
* [cite_start]**数据格式**：返回包含 `image`、`title`、`link` 的 JSON 对象 [cite: 24]。
* [cite_start]**兼容性**：支持跨域资源共享 (CORS) 与凭证携带 [cite: 23]。

## 🛠️ 技术栈
* **后端**: Java Servlet, MySQL, Gson
* **前端**: JSP, Vanilla JS (Fetch API)
* [cite_start]**数据库**: 19 个标准广告分类（涵盖数码、教育、金融、时尚等）[cite: 30, 46]

## 🚀 API 调用示例
[cite_start]`GET /ads/api/getAd?siteType=news&channel=教育&visitorId=5fd22056...` [cite: 22]